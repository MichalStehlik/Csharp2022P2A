// Enum

Color bg = Color.White;
Console.WriteLine(bg);
Console.WriteLine((int)bg);

// Operatory
// unarni
// !x
// binarni
// a + b
// ternarni
int a = 0;
int c = 4;
Console.WriteLine(a == 0 ? "Nic" : "Neco");
int b = 12 + --c + ((a == 0) ? 2 : 4);
public enum Color {
    Red,
    Green,
    Blue,
    White,
    Black
}

public enum Gender
{
    Unknown = 0,
    Male = 1,
    Female = 2,
    Other = 3
}
